# Core requirements (required for all platforms)
pycryptodome>=3.19.0
tqdm>=4.66.1
colorama>=0.4.6
psutil>=5.9.7

# GPU acceleration
numpy>=1.24.0
pyopencl>=2023.1.4; platform_system != "Darwin" or platform_machine != "arm64"  # For non-M1/M2/M3 Macs
metal-python>=1.0.0; platform_system == "Darwin" and platform_machine == "arm64"  # For M1/M2/M3 Macs

# Intel specific optimizations
intel-openmp>=2024.0.0; platform_machine == "x86_64"
mkl>=2024.0.0; platform_machine == "x86_64"
numba>=0.58.1; platform_machine == "x86_64"

# Apple Silicon specific optimizations
tensorflow-macos>=2.15.0; platform_system == "Darwin" and platform_machine == "arm64"
accelerate>=0.25.0; platform_system == "Darwin" and platform_machine == "arm64"

# Optional - for better performance
cython>=3.0.7